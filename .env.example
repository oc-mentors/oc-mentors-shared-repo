# Firebase – copy to .env.local and fill with values from Firebase Console
# Get these from: Project settings → Your apps → Web app config

VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=
VITE_FIREBASE_MEASUREMENT_ID=

# Study Hub Socratic tutor (UCI ZotGPT gpt-4o) — pick one or both:
# (A) Local dev: api-key from https://azureapi.zotgpt.uci.edu
VITE_ZOTGPT_API_KEY=
# (B) Production: firebase functions:secrets:set ZOTGPT_API_KEY && firebase deploy --only functions:socraticStudyChat

# Anteater API (ICSSC) — optional; most GETs work without a key. Key unlocks /search + higher rate limits.
# https://docs.icssc.club/docs/developer/anteaterapi/keys-limits
VITE_ANTEATER_API_KEY=
